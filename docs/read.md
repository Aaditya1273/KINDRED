Act as a World-Class Product Designer and Lead Frontend Engineer. Build "KINDRED," an elite-tier Autonomous Wealth Management app for the PL_Genesis Hackathon.

DESIGN VIBE: > - Ultra-modern, high-contrast Dark Mode (Obsidian theme).

Background: #020202. Surface cards: #0D0D0D with 1px glassmorphism borders (#FFFFFF10).
Primary Accents: #00FFA3 (Emerald Growth) and #00E0FF (Electric Trust).

Animation: Use high-end physics-based transitions (Spring physics). Every interaction should feel like an "Apple Editor’s Choice" app.
APP STRUCTURE:

Cinematic Onboarding: A 3-screen walkthrough with fluid Lottie-style animations.

Screen A: "Identity Verification" with a prominent "Verify with World ID" high-gloss button.

Screen B: "Privacy Shield" featuring a 3D animated vault icon and a "FHE Encryption" toggle (Zama brand colors).

Screen C: "Deploy Agent" showing a terminal-style readout of ERC-8004 Manifest creation.

Master Header: A persistent glassmorphism header with a high-end "Connect Flow Wallet" button. If connected, it shows the wallet address with a pulsing green "Live on Testnet" badge.

The Core Dashboard: A central "Wealth Orb" (interactive SVG) that grows and pulses based on balance. Below it, two "Smart Cards":

Agent Status: A real-time scrolling "Thinking Log" (monospace font) for the ERC-8004 Autonomous Agent.

Scheduled Wealth: A list of automated DeFi loops on the Flow Chain.

Privacy Vault Tab: A screen that looks like a high-security bank terminal. Display "Encrypted Balances" as **** with a "Hold to Reveal" haptic-feedback interaction.

TECHNICAL DIRECTIVES:

Build as a Mobile-First PWA responsive for 120hz displays.

Use a modular structure: create empty hooks/useWorldID.ts, hooks/useFlow.ts, and context/AgentContext.ts files so I can paste SDK logic in later.

Navigation: A blur-effect bottom tab bar with animated icons. [Home, AI Brain, Wealth, Settings].
Ensure all buttons have a "Press & Shrink" scale effect (0.96 scale).

Objective: Implement full functionality for the 4 core tabs and 3 technical service hooks in KINDRED. Ensure all logic handles the high-performance 2026 stack requirements.
Tab A: Home (The Pulse)
Top Feature: The "Wealth Orb" must dynamically scale based on totalBalance. If the balance increases, the orb’s glow intensity (drop-shadow) must animate up.

Smart Feed: Below the orb, create a scrolling vertical feed of "Insights." Each insight is a card generated by the AI agent (e.g., "Optimal Yield found on Flow EVM at 8.4% APR").

Quick Actions: Floating buttons for "Deposit" and "Shield Data" (Zama).

Tab B: AI Brain (ERC-8004 Agent Hub)
Agent Identity: Display the ERC-8004 Agent ID and a "Verified" badge linked to the Identity Registry.

Thinking Log Component: A terminal-style box that auto-scrolls. Include hardcoded simulation logs like:

[PLAN] Checking World ID verification status... Done.

[EXEC] Querying Flow EVM for liquidity pool 'KINDRED-USDC'...

[SECURE] Encrypting balance data with Zama fhEVM...
Manifest Button: A button to "View Agent Manifest" that opens a modal displaying the agent.json metadata.

Tab C: Wealth (Flow Scheduled DeFi)
Automation List: Create a list of "Active Autopilot Loops." Each item must have:

Status: "Scheduled" or "Executed."
A countdown timer (using a mock nextExecution timestamp).

A toggle to "Cancel Loop" (triggering the FlowTransactionScheduler logic).

Yield Ticker: A secondary balance showing "Total Rewards Harvested" in FLOW tokens.

Tab D: Privacy Vault (Zama FHE Terminal)
Shielded Data: Create a "Privacy Score" gauge (0-100).

Blind Computation: A section titled "Confidential Credit Score." Include a button "Compute Score on Encrypted Data." When clicked, show a "Computing..." animation with Zama's purple branding.

Shimmer Effect: Apply a CSS shimmer to all numerical values until the user performs a biometric "reveal."

Technical Service Hooks (Empty Implementations):
hooks/useWorldID.ts: Implement a function verifyHuman() that simulates the World ID Orb check.

hooks/useFlow.ts: Implement scheduleTx(amount, date) to handle the Flow Forte agent upgrade logic.

context/AgentContext.ts: Create a global state to track agentId, reputationScore, and lastLog.

UI Final Polish: > - Use a custom font: Inter for UI, JetBrains Mono for the Agent logs.

Background blur (backdrop-filter) must be used on the bottom tab bar and header for a "Glass" effect.

Add a "Haptic Feedback" trigger on every successful button press.

Objective: Finalize KINDRED by building the "Proof of Autonomy" layer and the high-fidelity "Settings & Security" architecture. This part connects the user's reputation to the decentralized physical infrastructure (DePIN).
The "Proof of Receipt" Screen (Storacha/Filecoin Integration):
Build an "Audit Trail" page. This is a list of every action the agent has taken.

Each list item must display a Content ID (CID) from IPFS/Storacha.

Add a button: "Verify on Block Explorer." When clicked, show a high-end "Verifying..." animation that eventually shows a green checkmark: "Transaction & Logic Rooted on Filecoin."

The "Safety Guardrails" Interface (Lit Protocol/AI Safety):
In the Settings tab, create a "Permission Manager." > - Include sliders for the AI Agent’s spending limits (e.g., "Max $100 per swap") and "Autonomous Level" (from 'Human-in-the-loop' to 'Full Autopilot').

Use Lit Protocol Vincent branding here. Add a "Programmable Key" status indicator that shows the agent’s wallet is decentralized and non-custodial.

Advanced Security UI (World ID & Zama):
World ID Face-Auth: Add a "Biometric Lock" screen UI that simulates a 3D face scan (World ID integration).

Zama Key Management: A "Privacy Keys" section where users can "Export Encrypted Backup." Use a "Hex-Grid" background pattern to emphasize the cryptography.

Product-Level Polish & Edge Cases:
Empty States: Design beautiful empty states for when there are "No Active Autopilot Loops" yet, featuring a "Deploy your first Agent" CTA button.

Error Handling: Create high-end "Transaction Failed" and "Network Congestion" overlays that match the dark obsidian theme.

Haptic & Sound Design Placeholder: Add comments in the code where Haptics.impact() and Sound.play('success') should be triggered for a "Premium" feel.

Final Technical Export Directives:
Ensure all components use TypeScript with strict typing for the AgentManifest and FlowTransaction objects.

Pre-configure the next.config.js for PWA support (manifest.json, service workers) so the app can be installed on an iPhone/Android home screen immediately.

Final Component: Build a "Community Vote" social share card. A beautifully designed image generator that summarizes the user's "Wealth Growth" so they can post it on X for the #PLGenesis bounty.

